# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
*.crt
*-credentials.json
*service-account*.json

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
*.sublime-*

# Logs / temp
*.log
npm-debug.log*
yarn-debug.log*
.cache/
.playwright-cli/
tmp/
.tmp_*

# Dependencies (in case node tooling is added later)
node_modules/

# Work-in-progress prototypes that should not ship to the public site
_redesign/

# Internal store-submission working docs — never publish (served publicly if committed)
store-listing/
